# DG-LAB Bukkit config

websocket:
  # 是否启用 WebSocket 服务器
  enabled: false
  # 二维码中使用的地址，通常应填写你的 MC 服务器公网 IP 或域名
  address: "127.0.0.1"
  # WebSocket 端口
  port: 8080
  # 如果为 true，二维码将使用 wss:// 协议
  useHttps: false
